import styles from "./Footer.module.css"

export function Footer() {
    return (
        <div className={styles.footer}>
            <p className={styles.link}>Footer</p>
        </div>
    );
}

export default Footer;